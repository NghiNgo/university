Chào bạn, đây là lời giải chi tiết cho Bài tập 3.11.

Để giải bài toán này bằng đệ quy, chúng ta sẽ lặp đi lặp lại việc so sánh **chữ số cuối cùng** với **chữ số lớn nhất của phần còn lại**.

Ví dụ, để tìm chữ số lớn nhất của số `274`:

1.  Ta so sánh chữ số cuối (4) với chữ số lớn nhất của phần còn lại (27).
2.  Để tìm chữ số lớn nhất của 27, ta so sánh chữ số cuối (7) với chữ số lớn nhất của phần còn lại (2).
3.  **Phần cơ sở:** Chữ số lớn nhất của 2 là 2 (vì `n < 10`).
4.  Quay lại: So sánh 7 và 2. 7 lớn hơn.
5.  Quay lại: So sánh 4 và 7. 7 lớn hơn.
6.  Kết quả: 7.

-----

### 🖥️ Code C++: Tìm Chữ Số Lớn Nhất (Đệ quy)

```cpp
/*
 * Giai Bai tap 3.11: Tim chu so lon nhat cua so nguyen duong (De quy)
 */
#include <iostream>

using namespace std;

/**
 * @brief Ham de quy tim chu so lon nhat cua mot so nguyen duong
 * @param n So nguyen duong can tim
 * @return Chu so lon nhat trong so n
 */
int TimChuSoLonNhat_DeQuy(int n) {
    
    // --- Phan co so (Base Case) ---
    // Neu n chi co 1 chu so (n < 10), chu so lon nhat la chinh no.
    if (n < 10) {
        return n;
    }

    // --- Phan de quy (Recursive Step) ---
    
    // 1. Lay chu so cuoi cung (last digit)
    // Vi du: 274 -> 4
    int chuSoCuoi = n % 10;

    // 2. Goi de quy de tim chu so lon nhat cua phan con lai (rest)
    // Vi du: Tim max cua 27
    int maxPhanConLai = TimChuSoLonNhat_DeQuy(n / 10);
    
    // 3. So sanh va tra ve ket qua
    // So sanh chuSoCuoi (4) voi maxPhanConLai (7)
    if (chuSoCuoi > maxPhanConLai) {
        return chuSoCuoi;
    } else {
        return maxPhanConLai;
    }
}


// ===================================================================
// Ham main de kiem tra
// ===================================================================
int main() {
    int n;
    
    cout << "Nhap mot so nguyen duong: ";
    cin >> n;

    // Dam bao so la duong
    if (n < 0) {
        n = -n; // Chuyen sang so duong
    }

    // Goi ham de quy
    int maxDigit = TimChuSoLonNhat_DeQuy(n);
    
    cout << "Chu so lon nhat trong " << n << " la: " << maxDigit << endl;

    // --- Vi du khac ---
    int n2 = 27493;
    cout << "\nSo: " << n2 << endl;
    cout << "Chu so lon nhat la: " << TimChuSoLonNhat_DeQuy(n2) << endl; // Mong doi: 9
    
    int n3 = 888;
    cout << "\nSo: " << n3 << endl;
    cout << "Chu so lon nhat la: " << TimChuSoLonNhat_DeQuy(n3) << endl; // Mong doi: 8

    return 0;
}
```