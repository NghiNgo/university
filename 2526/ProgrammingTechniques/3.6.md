Chào bạn, đây là lời giải cho Bài tập 3.6.

Để viết hàm này bằng đệ quy, chúng ta cần định nghĩa bài toán dựa trên "bài toán con" nhỏ hơn.

Chiều dài của một chuỗi (ví dụ: "Hello") có thể được định nghĩa là:
**1** (cho ký tự 'H') + **chiều dài của chuỗi còn lại** ("ello")

Chúng ta lặp lại điều này cho đến khi gặp bài toán cơ sở.

  * **Phần đệ quy (Recursive Step):** `Length(str) = 1 + Length(str + 1)`
  * **Phần cơ sở (Base Case):** Chiều dài của một chuỗi rỗng (`""`) là `0`. Chúng ta nhận biết chuỗi rỗng (hoặc kết thúc của một chuỗi) bằng ký tự `'\0'` (null terminator).

-----

### 🖥️ Code C++: Tính Chiều Dài Chuỗi Đệ Quy

```cpp
/*
 * Giai Bai tap 3.6: Tinh chieu dai chuoi (strlen) bang de quy
 */
#include <iostream>

using namespace std;

/**
 * @brief Tinh chieu dai cua mot chuoi C-style (char*) bang de quy.
 * @param str Con tro den ky tu dau tien cua chuoi.
 * @return Chieu dai cua chuoi (so luong ky tu truoc '\0').
 */
int TinhChieuDai_DeQuy(char* str) {
    
    // --- Phan co so (Base Case) ---
    // Neu ky tu ma con tro 'str' dang tro toi la ky tu KET THUC ('\0'),
    // chung ta da den cuoi chuoi (hoac chuoi rong).
    // Chieu dai cua chuoi "rong" nay la 0.
    if (*str == '\0') {
        return 0;
    }

    // --- Phan de quy (Recursive Step) ---
    // Neu chuoi chua ket thuc, chieu dai se bang:
    // 1 (dem ky tu hien tai)
    // +
    // chieu dai cua phan con lai cua chuoi (bat dau tu ky tu tiep theo).
    // (str + 1) la mot con tro tro den ky tu tiep theo.
    return 1 + TinhChieuDai_DeQuy(str + 1);
}


// ===================================================================
// Ham main de kiem tra
// ===================================================================
int main() {
    // Khai bao mot chuoi C-style.
    // Trong bo nho, chuoi nay la: 'H', 'e', 'l', 'l', 'o', '\0'
    char myString[] = "Hello"; 
    
    cout << "Chuoi: " << myString << endl;

    // Goi ham de quy
    int chieuDai = TinhChieuDai_DeQuy(myString);
    
    cout << "Chieu dai (de quy): " << chieuDai << endl; // Mong doi ket qua: 5

    // --- Kiem tra voi chuoi rong ---
    char emptyString[] = ""; // Trong bo nho, chuoi nay chi la: '\0'
    
    cout << "\nChuoi: " << "(chuoi rong)" << endl;
    chieuDai = TinhChieuDai_DeQuy(emptyString);
    cout << "Chieu dai (de quy): " << chieuDai << endl; // Mong doi ket qua: 0

    return 0;
}
```
Chào bạn, câu hỏi của bạn rất hay.

Đúng vậy, chúng ta **hoàn toàn có thể** viết hàm này mà không dùng cú pháp con trỏ (`char*`).

Lý do `char* str` được dùng là vì trong C/C++, khi bạn truyền một mảng (như `char myString[]`), nó tự động được "giảm" (decay) thành một con trỏ trỏ đến phần tử đầu tiên của mảng. Hàm `TinhChieuDai_DeQuy(str + 1)` là một cách dùng "phép toán con trỏ" để nói "gọi lại hàm với chuỗi bắt đầu từ ký tự tiếp theo".

Tuy nhiên, có một cách khác rõ ràng hơn về mặt khái niệm, đó là **truyền mảng và một chỉ số (index)**.

### 💡 Cách 2: Dùng Chỉ Số (Index) để Đệ Quy

Thay vì "cắt" chuỗi, chúng ta giữ nguyên chuỗi và truyền thêm một biến `index` để theo dõi vị trí chúng ta đang xét.

  * **Logic:** Chiều dài của chuỗi = 1 + Chiều dài của chuỗi bắt đầu từ `index + 1`.
  * **Phần cơ sở:** Nếu ký tự tại `str[index]` là `'\0'`, ta đã ở cuối chuỗi, chiều dài của phần này là 0.
  * **Phần đệ quy:** Nếu chưa, ta cộng 1 (đếm ký tự `str[index]`) và gọi đệ quy với `index + 1`.

-----

### 🖥️ Code C++ (Không dùng cú pháp con trỏ)

Đây là cách triển khai:

```cpp
/*
 * Giai Bai tap 3.6: Tinh chieu dai chuoi (De quy)
 * Phien ban KHONG DUNG C-U PHAP CON TRO (char*)
 * Thay vao do, chung ta dung mang (char[]) va chi so (index)
 */
#include <iostream>

using namespace std;

/**
 * @brief Ham "helper" (ho tro) de quy co them chi so
 * @param str Mang ky tu (chuoi)
 * @param index Vi tri (chi so) hien tai chung ta dang xet
 * @return Chieu dai cua chuoi tinh tu 'index' tro ve cuoi
 */
int TinhChieuDai_Helper(char str[], int index) {
    
    // --- Phan co so (Base Case) ---
    // Neu ky tu tai chi so 'index' la ky tu KET THUC ('\0')
    if (str[index] == '\0') {
        return 0;
    }

    // --- Phan de quy (Recursive Step) ---
    // Chieu dai = 1 (dem ky tu hien tai)
    // +
    // Chieu dai cua phan con lai (goi de quy voi index + 1)
    return 1 + TinhChieuDai_Helper(str, index + 1);
}

/**
 * @brief Ham "bao" (wrapper) chinh
 * Ham nay duoc tao ra de nguoi dung su dung de dang hon,
 * khong can phai quan tam den 'index'.
 * @param str Mang ky tu (chuoi)
 * @return Tong chieu dai cua chuoi
 */
int TinhChieuDai_DeQuy(char str[]) {
    // Bat dau de quy tu chi so dau tien (index = 0)
    return TinhChieuDai_Helper(str, 0);
}


// ===================================================================
// Ham main de kiem tra
// ===================================================================
int main() {
    // Khai bao mot chuoi C-style (thuc chat la mang char)
    char myString[] = "Hello"; 
    
    cout << "Chuoi: " << myString << endl;

    // Goi ham "bao", cach su dung giong het nhu truoc
    int chieuDai = TinhChieuDai_DeQuy(myString);
    
    cout << "Chieu dai (de quy, khong con tro): " << chieuDai << endl; // Mong doi: 5

    return 0;
}
```
