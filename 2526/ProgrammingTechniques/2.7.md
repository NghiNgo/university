Chào bạn, đây là lời giải chi tiết cho Bài tập 7.

Bài tập này là sự kết hợp tuyệt vời giữa hai kỹ thuật chúng ta đã học:

1.  **Tham số hàm `main`** (giống `Compute.exe` trong Bài 1.4).
2.  **Đọc file từng ký tự** (giống `read_text_file.cpp` trong Bài 1).

Chương trình sẽ nhận tên file từ đối số dòng lệnh (`argv[1]`) và truyền tên file đó vào hàm hiển thị.

-----

### 🖥️ Bài 7: Chương Trình `Show.exe` (Hiển Thị File)

Đây là code C++ cho chương trình. Bạn có thể lưu file này với tên `Show.cpp`.

```cpp
/*
 * Giai Bai tap 7: Chuong trinh Show.exe
 * Hien thi noi dung file van ban bang doi so dong lenh (main arguments).
 */
#include <stdio.h>
#include <stdlib.h> // De dung ham exit()

/**
 * @brief Doc tung ky tu cua file va in ra man hinh
 * @param fname Duong dan va ten file can doc
 * (Ham nay giong voi ham trong Bai tap 1)
 */
void HienThiFile(char fname[]) {
    // 1. Khai bao con tro file
    FILE *fp;

    // 2. Mo file o che do "rt" (read text - doc van ban)
    fp = fopen(fname, "rt");

    // 3. Kiem tra mo file co thanh cong khong
    if (fp == NULL) {
        printf("Loi: Khong the mo file %s.\n", fname);
        exit(1); // Thoat chuong trinh neu file loi
    }
    
    // 4. Doc file tung ky tu cho den khi het file (feof)
    while (!feof(fp)) {
        // Doc mot ky tu duy nhat tu file
        char c = fgetc(fp);

        // Kiem tra xem da thuc su het file chua (tranh in ra ky tu la)
        if (c != EOF) {
            // In ky tu do ra man hinh
            printf("%c", c);
        }
    }

    // 5. Dong file
    fclose(fp);
}


/**
 * @brief Ham main xu ly cac doi so dong lenh
 * @param argc (Argument Count): So luong doi so (luon >= 1)
 * @param argv (Argument Vector): Mang chuoi, chua cac doi so
 */
int main(int argc, char *argv[]) {
    
    // 1. Kiem tra so luong doi so
    // Chung ta mong doi 2 doi so:
    // argv[0]: Ten chuong trinh (vi du: "Show.exe")
    // argv[1]: Ten file can hien thi (vi du: "dssv.txt")
    // => Vay argc phai bang 2.
    
    if (argc != 2) {
        // Neu so luong sai, in huong dan su dung
        printf("Loi: Sai cu phap.\n");
        printf("Cach dung: Show.exe <ten_file_can_hien_thi>\n");
        printf("Vi du:     Show.exe TiengThu_LuuTrongLu.txt\n");
    } else {
        // 2. Neu so luong dung (argc == 2)
        // Doi so chung ta can la ten file, nam o argv[1]
        char* tenFile = argv[1];
        
        // 3. Goi ham HienThiFile de xu ly
        HienThiFile(tenFile);
    }

    return 0;
}
```

-----

### 📋 Hướng Dẫn Sử Dụng (Bắt buộc)

Vì đây là chương trình dùng dòng lệnh, bạn không thể chạy nó bằng cách nhấn nút "Run" trong IDE. Bạn phải thực hiện các bước sau:

**1. Biên dịch chương trình:**
Mở Terminal (hoặc CMD) tại thư mục chứa file `Show.cpp` và gõ lệnh sau:

```bash
g++ Show.cpp -o Show.exe
```

Lệnh này sẽ tạo ra file `Show.exe`.

**2. Chuẩn bị file dữ liệu:**
Đảm bảo bạn có các file `.txt` (như `TiengThu_LuuTrongLu.txt` hoặc `dssv.txt`) trong cùng thư mục.

**3. Chạy chương trình:**
Tại cửa sổ Terminal, gõ lệnh `Show.exe` theo sau là tên file bạn muốn xem:

```bash
C:\HocTap> Show.exe TiengThu_LuuTrongLu.txt
```

**4. Kết Quả:**
Chương trình sẽ in ra nội dung của file `TiengThu_LuuTrongLu.txt`:

```bash
C:\HocTap> Show.exe TiengThu_LuuTrongLu.txt
Tieng Thu

...
Em co nghe mua thu
La thu roi xao xac
Con nai vang ngo ngac,
Dap tren la vang kho...

-- Luu Trong Lu, 1939 --
```

**Lưu ý Tiếng Việt:** Nếu file của bạn có tiếng Việt có dấu (lưu ở UTF-8), hãy gõ lệnh `chcp 65001` vào Terminal trước khi chạy `Show.exe` để đảm bảo hiển thị đúng font chữ.