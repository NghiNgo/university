Chào bạn, đây là lời giải cho Bài 1, cùng với giải thích về cách đọc file tiếng Việt có dấu.

### 📜 Bài 1: Đọc và Hiển thị File Văn Bản ASCII

Đây là chương trình C++ để đọc và hiển thị nội dung của một file văn bản. Chương trình này sử dụng kỹ thuật đọc từng ký tự, dựa trên **Code Mẫu 3** (từ file `read_text_file.cpp` bạn đã cung cấp).

```cpp
/*
 * Giai Bai tap 1: Doc noi dung file van ban va hien thi ra man hinh
 * Vi du: doc file "TiengThu_LuuTrongLu.txt"
 */
#include <stdio.h>
#include <stdlib.h> // De dung ham exit()

/**
 * @brief Doc tung ky tu cua file va in ra man hinh
 * @param fname Duong dan va ten file can doc
 */
void DocVaHienThiFile(char fname[]) {
    // 1. Khai bao con tro file
    FILE *fp;

    // 2. Mo file o che do "rt" (read text - doc van ban)
    fp = fopen(fname, "rt");

    // 3. Kiem tra mo file co thanh cong khong
    if (fp == NULL) {
        printf("Loi: Khong the mo file %s.\n", fname);
        printf("Vui long kiem tra lai duong dan va ten file.\n");
        exit(1); // Thoat chuong trinh neu file loi
    }

    // 4. In thong bao bat dau
    printf("--- Noi dung file %s ---\n\n", fname);
    
    // 5. Doc file tung ky tu cho den khi het file (feof)
    while (!feof(fp)) {
        // Doc mot ky tu duy nhat tu file
        char c = fgetc(fp);

        // Kiem tra xem da thuc su het file chua
        // fgetc se tra ve EOF (End Of File) khi het file
        if (c != EOF) {
            // In ky tu do ra man hinh
            printf("%c", c);
        }
    }

    // 6. In thong bao ket thuc va dong file
    printf("\n\n--- Ket thuc file ---\n");
    fclose(fp); // Luon dong file sau khi su dung xong
}

int main() {
    // Luu y:
    // - Duong dan file trong C/C++ can dung dau / (slash)
    // - hoac dau \\ (2 dau backslash)
    char tenFile[] = "TiengThu_LuuTrongLu.txt"; // File nay phai dat cung thu muc voi file .exe
    // char tenFile[] = "D:/BaiTap/TiengThu_LuuTrongLu.txt"; // Vi du ve duong dan tuyet doi
    
    DocVaHienThiFile(tenFile);

    return 0;
}
```

-----

### 🇻🇳 Làm thế nào để đọc file văn bản tiếng Việt có dấu?

Đây là một câu hỏi rất hay. Vấn đề này không nằm ở **cách đọc** của C++ mà nằm ở **cách mã hóa** ký tự.

1.  **File ASCII vs. File UTF-8:**

      * **ASCII** (như trong đề bài) chỉ bao gồm 128 ký tự cơ bản (chữ cái không dấu, số, ký tự đặc biệt).
      * **Tiếng Việt có dấu** cần nhiều ký tự hơn, và thường được lưu bằng bảng mã **UTF-8**.

2.  **Cách C++ Đọc File:**

      * Hàm `fgetc` của C++ không "đọc" ký tự; nó **đọc "byte"**.
      * Trong file UTF-8, một ký tự có dấu (như `ô` hay `ệ`) có thể được tạo thành từ **2 hoặc 3 bytes**.
      * Chương trình C++ ở trên vẫn đọc file UTF-8 một cách chính xác, nhưng nó đọc từng byte một.

3.  **Tại sao Bị Lỗi Font (Hiển thị Ký Tự Lạ)?**

      * Lỗi không phải do C++ đọc sai, mà do **màn hình console (Terminal/CMD) không biết** cách ráp các byte đó lại thành chữ tiếng Việt.
      * Mặc định, console của Windows dùng bảng mã cũ (như 437 hoặc 866), không hỗ trợ UTF-8.

#### ✅ Giải Pháp: Đặt Chế Độ Hiển Thị Của Console

Để console (Terminal/CMD) của bạn có thể hiển thị được tiếng Việt (UTF-8) mà file C++ đọc ra, bạn chỉ cần thực hiện 1 bước duy nhất:

**Trước khi chạy file `.exe` của bạn, hãy gõ lệnh sau vào console:**

```bash
chcp 65001
```

  * `chcp` là viết tắt của "Change Code Page".
  * `65001` là mã của **UTF-8**.

Sau khi chạy lệnh này, console của bạn sẽ chuyển sang chế độ UTF-8, và chương trình C++ của bạn (vẫn dùng code y hệt ở trên) sẽ hiển thị tiếng Việt có dấu một cách chính xác.

> **Tóm tắt:** Code C++ để đọc file là không đổi. Bạn chỉ cần **lưu file `.txt` của bạn ở định dạng UTF-8** và **chạy lệnh `chcp 65001`** trong console trước khi chạy chương trình.